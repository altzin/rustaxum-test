## Database & Migrations
at some point bring kubernetes migration runners. run as pre deploy step not in main
### Local Database
Start the local Postgres instance:
```fish
podman compose up -d postgres
